name = "go-testing"
description = "Run and debug Go tests using project conventions."
version = "0.1.0"

triggers = [
  "test",
  "go test",
  "failing test",
  "coverage"
]

allowed_tools = [
  "read_file",
  "search_text",
  "run_command"
]

context_budget_tokens = 4000
