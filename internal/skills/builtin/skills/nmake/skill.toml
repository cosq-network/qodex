name = "nmake"
description = "Build projects with Microsoft NMake on Windows."
version = "0.1.0"

triggers = [
  "nmake",
  "microsoft make",
  "windows build",
  "makefile windows",
  "msBuild alternative"
]

allowed_tools = [
  "nmake_build",
  "msbuild",
  "dotnet_build",
  "read_file",
  "search_text",
  "list_files",
  "run_command",
  "review_changes"
]

context_budget_tokens = 4000
