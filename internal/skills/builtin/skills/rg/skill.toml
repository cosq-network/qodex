name = "rg"
description = "Search files with ripgrep using structured arguments."
version = "0.1.0"

triggers = [
  "rg",
  "ripgrep",
  "search",
  "grep",
  "find text"
]

allowed_tools = [
  "rg_search",
  "read_file",
  "search_text",
  "list_files"
]

context_budget_tokens = 4000
